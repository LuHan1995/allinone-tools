# All-in-One 工程师工具箱 — v4.0 开发方案

> 版本：v4.0  
> 日期：2026-06-10  
> 目标：新增天气预报集成、隔离电源变压器计算器、ADC/DAC 信号链精度与噪声分析

---

## 一、本轮工作范围

基于 v3.0 已完成的功能，v4.0 聚焦以下三个高价值模块：

1. **万年历 + 本地天气预报集成** — 基于浏览器定位，免 Key 获取实时天气，国内网络友好
2. **隔离电源变压器计算器** — 覆盖反激/正激/推挽三大隔离拓扑，计算匝比、电感、峰值电流与器件应力
3. **ADC/DAC 信号链精度与噪声计算器** — 从分压/运放/滤波到 ADC/DAC 的全链路误差预算与噪声分析，输出有效位数与绝对电压精度

---

## 二、模块 1：万年历天气预报集成

### 2.1 需求分析

- 在现有万年历模块中增加实时天气显示，位于日历主卡片上方或侧边栏
- 自动获取用户位置，无需手动输入城市
- 天气数据源必须在国内网络环境下可直接访问，不依赖用户自备代理或 API Key
- 天气信息包含：当前温度、天气状况、风速、未来 3 天预报

### 2.2 技术方案

#### 定位策略（三层降级）

| 优先级 | 方案 | 精度 | 说明 |
|---|---|---|---|
| 1 | `navigator.geolocation.getCurrentPosition()` | 高（GPS/基站） | 浏览器原生，需用户授权 |
| 2 | IP 定位（太平洋电脑网 JSONP） | 中（城市级） | `https://whois.pconline.com.cn/ipJson.jsp?jsonReturn=true`，无跨域问题 |
| 3 | 手动输入城市 | 低 | 提供城市名搜索框作为兜底 |

#### 天气 API 选型

| 方案 | 地址 | 需 Key | 国内访问 | 决策 |
|---|---|---|---|---|
| **Open-Meteo** | `api.open-meteo.com` | 否 | 中等（Cloudflare CDN） | **主方案**：经纬度直查，免 Key，支持 7 天预报 |
| 和风天气 | `devapi.qweather.com` | 是 | 快（国内 CDN） | **备选**：设置页提供 Key 配置入口，供国内访问不畅时切换 |

**Open-Meteo 请求示例**：
```
https://api.open-meteo.com/v1/forecast
  ?latitude=39.9&longitude=116.4
  &current_weather=true
  &daily=weathercode,temperature_2m_max,temperature_2m_min
  &timezone=auto
```

**WMO Weather Code 映射**：返回的 `weathercode` 映射为中文天气描述 + Emoji 图标，无需外部图片资源。

#### 缓存策略

- 天气数据缓存在 `localStorage`，Key 为 `weather_cache_v1`
- 缓存有效期 30 分钟，避免频繁刷新页面时重复请求
- 缓存结构：`{ latitude, longitude, current, daily, updatedAt }`

### 2.3 UI 设计

在 `calendar.js` 的万年历 `card` 上方增加天气面板：

```
┌──────────────────────────────────────────────────────┐
│ 🌤️ 北京  26°C  晴  │  风速 15km/h  │  湿度 45%      │
│ 今天 26°/18°  ⛅ 明天 27°/19°  🌧️ 后天 24°/17°       │
└──────────────────────────────────────────────────────┘
```

- 移动端自动折叠为紧凑模式：只显示当前温度 + 天气图标 + 最高/最低
- 点击天气面板可手动刷新或切换城市（弹出简易设置浮层）

### 2.4 文件变更

| 文件 | 变更类型 | 说明 |
|---|---|---|
| `js/modules/calendar.js` | 修改 | 新增天气获取、缓存、渲染逻辑；增加位置授权处理 |

---

## 三、模块 2：隔离电源变压器计算器（Flyback / Forward / Push-Pull）

### 3.1 需求分析

- 覆盖三种最常用的隔离 DC-DC 拓扑：反激（Flyback）、正激（Forward）、推挽（Push-Pull）
- 给定输入/输出参数，计算变压器关键参数和器件应力
- 提供磁芯选型参考（AP 法），内置常用 E 型磁芯数据库
- UI 风格与现有 `dcdc-calc.js` 保持一致（Tab 切换 + 预设按钮 + 公式展示）

### 3.2 核心公式

#### 反激 Flyback（单端反激）

| 输出参数 | 公式 |
|---|---|
| 反射电压 | `Vr = N × (Vout + Vf)` |
| 占空比 | `D = Vr / (Vin + Vr)` |
| 初级电感 | `Lp = (Vin × D)² / (2 × Pout × fsw × Krf)` |
| 初级峰值电流 | `Ipk = (2 × Pout) / (Vin × D × η) + ΔIp/2` |
| 初级 RMS 电流 | `Ip_rms = Ipk × √(D/3)` (DCM 近似) |
| MOS 管电压应力 | `Vds_max = Vin_max + Vr + Vspike` |
| 整流管电压应力 | `Vd_max = Vout + Vin_max / N` |

#### 正激 Forward（单端正激）

| 输出参数 | 公式 |
|---|---|
| 匝比 | `N = (Vin_min × D_max) / (Vout + Vf)` |
| 占空比 | `D = (Vout + Vf) / (Vin / N)` |
| 输出滤波电感 | `Lout = ((Vin/N − Vout) × D) / (ΔIL × fsw)` |
| 复位绕组匝比 | `Nr = Np`（通常 1:1） |
| 最大占空比限制 | `D_max < Nreset / (Nreset + Np) = 0.5` |
| MOS 管电压应力 | `Vds_max = 2 × Vin_max` |

#### 推挽 Push-Pull

| 输出参数 | 公式 |
|---|---|
| 匝比 | `N = (Vin_min × 2 × D_max) / (Vout + Vf)` |
| 占空比 | `D = N × (Vout + Vf) / (2 × Vin)` |
| 输出滤波电感 | `Lout = ((Vin/N − Vout) × D) / (ΔIL × fsw)` |
| 最大占空比限制 | `D_max < 0.45`（留死区） |
| MOS 管电压应力 | `Vds_max = 2 × Vin_max` |

#### 磁芯选型（AP 法，三种拓扑通用）

```
AP = Aw × Ae = (Pt × 10⁴) / (Ku × Kj × ΔB × fsw × J)   [cm⁴]
```

其中：
- `Pt = Pout / η + Pout`（处理功率）
- `Ku = 0.4`（窗口利用系数）
- `Kj = 1.0`（电流波形系数，方波近似）
- `ΔB = 0.2~0.3 T`（磁通密度摆幅，铁氧体）
- `J = 4~6 A/mm²`（电流密度）

**内置磁芯数据库（E 型铁氧体）**：

| 型号 | Ae (mm²) | Aw (mm²) | AP (cm⁴) | Le (mm) |
|---|---|---|---|---|
| EE13 | 12.5 | 13.0 | 0.016 | 27.6 |
| EE16 | 19.8 | 19.0 | 0.038 | 30.2 |
| EE19 | 23.0 | 25.5 | 0.059 | 33.5 |
| EE22 | 42.0 | 31.0 | 0.130 | 39.0 |
| EE25 | 52.0 | 40.0 | 0.208 | 46.0 |
| EE30 | 60.0 | 60.0 | 0.360 | 55.5 |
| EE33 | 86.0 | 72.0 | 0.619 | 59.0 |
| EE40 | 127 | 109 | 1.38 | 76.0 |
| EE42 | 182 | 150 | 2.73 | 88.0 |
| EE55 | 250 | 220 | 5.50 | 102 |

### 3.3 UI 设计

参考 `dcdc-calc.js`，采用 Tab 切换三种拓扑。

```
┌─────────────────────────────────────────────────────┐
│ [反激 Flyback] [正激 Forward] [推挽 Push-Pull]       │
├─────────────────────────────────────────────────────┤
│ 输入参数                                              │
│ Vin_min / Vin_max / Vout / Iout / fsw / η / Vf       │
│ 纹波电流比例 % │ 预设: 20% 30% 40%                    │
│ 高级: [ ] 展开磁芯选型参数 (ΔB / J / Ku)              │
├─────────────────────────────────────────────────────┤
│ 计算结果                                              │
│ • 匝比 Np:Ns  =  xx : 1                              │
│ • 占空比 D    =  45.2 %  (范围: 28%~45%)             │
│ • 初级电感 Lp =  1.25 mH                             │
│ • 峰值电流    =  1.85 A                              │
│ • MOS 管耐压  ≥  650 V                               │
│ • 整流管耐压  ≥  100 V                               │
│ ─────────────────────────────────                   │
│ 推荐磁芯: EE25 (AP=0.208 cm⁴, 裕量 1.3x)             │
├─────────────────────────────────────────────────────┤
│ [展开公式与推导过程]                                  │
└─────────────────────────────────────────────────────┘
```

### 3.4 文件变更

| 文件 | 变更类型 | 说明 |
|---|---|---|
| `js/tools-data.js` | 修改 | 注册 `smps-transformer` 模块 |
| `js/modules/smps-transformer.js` | 新建 | 三种拓扑计算、磁芯数据库、AP 法选型 |
| `js/app.js` | 修改 | 拼音搜索映射增加"变/压/器/反/激/正/激/推/挽" |

---

## 四、模块 3：ADC/DAC 信号链精度与噪声计算器

### 4.1 需求分析

这是一个专业级信号链分析工具，目标用户是模拟/混合信号电路设计工程师。

**功能需求拆解**：

1. **INL/DNL 误差量化**：将 ADC/DAC 的 INL/DNL（单位：LSB）转换为电压误差
2. **运放误差分析**：Vos（输入失调电压）经放大后的输出误差；Gain Error 经信号传递后的误差
3. **级联带宽限制**：多级运放/滤波器级联后的总 -3dB 带宽
4. **信号链噪声计算**：
   - 热噪声（电阻）
   - 运放等效输入噪声（电压噪声密度 + 电流噪声密度）
   - ADC/DAC 量化噪声
   - 各噪声源经增益加权后的 RSS 叠加
5. **最终电压精度**：综合所有误差与噪声，输出有效位数（ENOB）、无噪声分辨率、绝对电压精度

### 4.2 信号链模型

#### 采集链路（ADC 信号链）

```
传感器/信号源
  → [分压器/衰减器] → [运放增益级] → [RC 滤波器] → [运放缓冲/驱动] → ADC
```

#### 输出链路（DAC 信号链）

```
DAC
  → [运放缓冲] → [RC 滤波器] → [功率运放/驱动] → 负载
```

#### 级类型定义

| 级类型 | 关键参数 | 对增益的贡献 | 对噪声的贡献 | 对带宽的贡献 |
|---|---|---|---|---|
| **分压器** | R1, R2 | `G = R2/(R1+R2)` | 热噪声 `√(4kTReqB)` | 无限制（∞） |
| **运放增益级** | G, Vos, GE%, BW, en (nV/√Hz), in (pA/√Hz), Rin, Rf | `G` | `en×√(BWn×π/2)` + `in×Req×√(BWn×π/2)` + 电阻热噪声 | `BW` |
| **RC 低通滤波器** | R, C | `G = 1` | 热噪声 `√(4kTRB)` | `BW = 1/(2πRC)` |
| **ADC** | N bit, Vref, INL(LSB), DNL(LSB), fs | 终止级 | 量化噪声 `LSB/√12` | `BW = 0.5×fs` (奈奎斯特) |
| **DAC** | N bit, Vref, INL(LSB), DNL(LSB), fupdate | 起始级 | 量化噪声 `LSB/√12` | `BW = 0.5×fupdate` |

### 4.3 核心计算模型

#### 1. 总增益

```
G_total = G₁ × G₂ × ... × Gₙ
```

对于采集链路，`G_total` 为电压增益（V/V）；对于输出链路，`G_total` 为 DAC 输出到负载的总传递系数（通常 ≤ 1 或放大）。

#### 2. 总带宽（一阶级联近似）

各级均为单极点一阶系统时：

```
BW_total = 1 / √( Σ(1/BWᵢ²) )
```

如果某级不是带宽限制级（如分压器），其 `BWᵢ = ∞`，该项为 0。

#### 3. 噪声计算（折合到输出端 RSS 叠加）

对于采集链路，噪声源折合到**信号链输出端（ADC 输入端）**：

```
Vn_total_out = √( Σ( Vnᵢ × (Gᵢ₊₁ × Gᵢ₊₂ × ... × Gₙ) )² + Vn_ADC² )
```

其中：
- **运放噪声电压密度**：`en [nV/√Hz]`，等效噪声带宽 `BWn = BW_total × π/2`（一阶系统）
  - 运放电压噪声贡献：`Vn_v = en × √(BWn)`
  - 运放电流噪声贡献：`Vn_i = in × Req × √(BWn)`，其中 `Req` 为等效输入电阻
- **电阻热噪声**：`Vn_R = √(4kTR × BWn)`，其中 `k = 1.38×10⁻²³ J/K`，`T = 300K`
- **ADC 量化噪声**：`Vn_q = LSB / √12 = Vref / (2ᴺ × √12)`

对于输出链路，噪声源折合到**负载端**：

```
Vn_total_load = √( Vn_DAC² × G_post² + Σ(Vnᵢ × G_afterᵢ)² )
```

#### 4. INL/DNL 误差

- **INL 误差电压**：`Err_INL = INL_max × LSB`（确定性系统误差，表示传输函数偏离理想直线的最大偏差）
- **DNL 误差电压**：`Err_DNL = DNL_max × LSB`（码跳变时的步长不确定性，表现为量化间隙的抖动）

在精度预算中，INL 通常作为**系统误差**直接叠加（ worst-case 或 RSS），DNL 作为**随机误差**与噪声 RSS 叠加。

#### 5. 运放 Vos 和 Gain Error 误差

- **Vos 输出误差**（每级）：`Err_Vos_i = Vos_i × G_noise_i`，其中 `G_noise_i = 1 + Rf/Rin`（噪声增益，同相/反相均适用）
- **总 Vos 误差折合到输出**：各级 Vos 误差经后级增益放大后 RSS 叠加
  ```
  Err_Vos_total = √( Σ( Err_Vos_i × (Gᵢ₊₁ × ... × Gₙ) )² )
  ```
- **Gain Error 误差**（满量程 worst-case）：`Err_GE = VFS × GE%`
  - 其中 `VFS` 为信号链满量程输出电压（通常 = Vref）
  - `GE%` 为各级增益误差的 RSS：`GE_total = √(GE₁² + GE₂² + ...)`

#### 6. 最终精度与有效分辨率

- **总随机误差（RMS）**：`Err_random = √(Vn_total² + Err_DNL²)`
- **总系统误差**：`Err_system = Err_INL + Err_Vos_total + Err_GE`
- **峰峰值噪声（99.9% 置信度）**：`Vn_pp = 6.6 × Vn_total`（高斯分布）
- **无噪声分辨率（Noise-Free Resolution）**：
  ```
  NFCR = log2( VFS / Vn_pp )
  ```
- **有效位数 ENOB（基于噪声）**：
  ```
  ENOB = log2( VFS / (Vn_total × √12 × 2) ) = (VFS_dB − Noise_floor_dB − 1.76) / 6.02
  ```
  简化计算：`ENOB = log2( VFS / (Vn_total × 2√3) )`
- **绝对电压精度（±，25°C）**：
  ```
  Accuracy = ±(Err_system + 3 × Err_random)   [3σ 覆盖 99.7%]
  ```

### 4.4 UI 设计

采用"链路方向 + 可增删级"的垂直卡片布局。

```
┌──────────────────────────────────────────────────────────┐
│ 信号链方向:  [● 采集链路（→ ADC）]  [○ 输出链路（DAC →）]  │
├──────────────────────────────────────────────────────────┤
│ ┌─────────────┐                                          │
│ │ ① 分压器    │  G=0.5  │  删除                        │
│ │ R1=10k R2=10k                                          │
│ └─────────────┘                                          │
│       ↓                                                  │
│ ┌─────────────┐                                          │
│ │ ② 运放增益  │  G=10   │  删除                        │
│ │ Vos=±50µV  GE=±0.1%  BW=1MHz  en=4nV/√Hz             │
│ └─────────────┘                                          │
│       ↓                                                  │
│ ┌─────────────┐                                          │
│ │ ③ RC 滤波   │  G=1    │  删除                        │
│ │ R=100Ω  C=100nF  (fc=15.9kHz)                         │
│ └─────────────┘                                          │
│       ↓                                                  │
│ ┌─────────────┐                                          │
│ │ ④ ADC       │  终止级  │  删除                        │
│ │ 16bit  Vref=3.3V  INL=±2LSB  DNL=±0.5LSB  fs=1MSPS   │
│ └─────────────┘                                          │
│                                                          │
│ [+ 添加级]  类型: [分压器 ▼]                            │
├──────────────────────────────────────────────────────────┤
│ 📊 计算结果                                               │
│ ┌─────────────────┬─────────────────┐                   │
│ │ 总增益          │ 5.00 V/V (14 dB) │                   │
│ │ 总带宽          │ 15.8 kHz        │                   │
│ │ 满量程 VFS      │ 3.300 V         │                   │
│ ├─────────────────┴─────────────────┤                   │
│ │ 噪声预算 (折合到 ADC 输入端)        │                   │
│ │ • 分压器热噪声  │  0.51 µVrms    │                   │
│ │ • 运放噪声      │ 12.35 µVrms    │                   │
│ │ • RC 热噪声     │  0.16 µVrms    │                   │
│ │ • ADC 量化噪声  │ 35.59 µVrms    │                   │
│ │ ─────────────────────────────────  │                   │
│ │ 总噪声 RSS      │ 37.71 µVrms    │                   │
│ │ 峰峰值噪声(6.6σ)│ 248.9 µVpp     │                   │
│ ├─────────────────┴─────────────────┤                   │
│ │ 误差预算                            │                   │
│ │ • INL 误差      │ ±71.2 µV       │                   │
│ │ • Vos 误差      │ ±500 µV        │                   │
│ │ • Gain Error    │ ±3.30 mV (1%)  │                   │
│ ├─────────────────┴─────────────────┤                   │
│ │ 🔑 最终精度                         │                   │
│ │ 理论分辨率      │ 16.00 bit      │                   │
│ │ 有效位数 ENOB   │ 14.12 bit      │                   │
│ │ 无噪声分辨率    │ 12.05 bit      │                   │
│ │ 最小可辨电压    │ 248.9 µV (pp)  │                   │
│ │ 绝对精度 (±3σ)  │ ±3.41 mV       │                   │
│ └───────────────────────────────────┘                   │
│ [展开详细计算过程与公式]                                  │
└──────────────────────────────────────────────────────────┘
```

**交互细节**：
- 每级卡片可折叠/展开，折叠时只显示类型和增益
- 点击"删除"移除该级，最少保留 2 级（前端调理 + ADC/DAC）
- "添加级"下拉框可选择级类型，选择后插入到链路末尾（ADC/DAC 之前）
- 所有参数修改实时重算（debounce 100ms）

### 4.5 文件变更

| 文件 | 变更类型 | 说明 |
|---|---|---|
| `js/tools-data.js` | 修改 | 注册 `signal-chain` 模块；将现有 `adc-calc` 标记为 deprecated 或保留简化版 |
| `js/modules/signal-chain.js` | 新建 | 信号链级联配置、噪声计算、误差预算、ENOB 计算 |
| `js/utils.js` | 修改 | 新增物理常量 `PHYSICAL_CONSTANTS`（k, T, q）、噪声计算辅助函数 `calcNoiseBW`、`calcThermalNoise` |
| `js/app.js` | 修改 | 拼音搜索映射增加"信/号/链/精/度/噪/声/采/集/输/出/INL/DNL/ENOB" |

---

## 五、公共改进与工具支持

### 5.1 `js/utils.js` 新增函数

```javascript
// 物理常量
export const PHYSICAL = {
  k: 1.380649e-23,    // 玻尔兹曼常数 J/K
  T0: 300,            // 室温 K (27°C)
  q: 1.602176634e-19, // 元电荷 C
};

// 一阶级联噪声带宽: BWn = BW_3dB × π/2
export function equivNoiseBW(bw3db) { return bw3db * Math.PI / 2; }

// 热噪声电压: Vn = sqrt(4kTRB)
export function thermalNoise(R, T, bw) { return Math.sqrt(4 * PHYSICAL.k * T * R * bw); }

// RSS 叠加: sqrt(v1² + v2² + ...)
export function rss(values) { return Math.sqrt(values.reduce((s, v) => s + v*v, 0)); }

// ENOB 计算（基于总噪声 RMS 和满量程）
export function calcENOB(vfs, noiseRms) {
  return Math.log2(vfs / (noiseRms * 2 * Math.sqrt(3)));
}
```

### 5.2 拼音搜索映射更新

在 `js/app.js` 的 `PINYIN_MAP` 中追加：

```javascript
'天': 't', '气': 'q', '预': 'y', '报': 'b',
'变': 'b', '压': 'y', '器': 'q', '反': 'f', '激': 'j', '正': 'z', '推': 't', '挽': 'w',
'信': 'x', '号': 'h', '链': 'l', '精': 'j', '度': 'd', '噪': 'z', '声': 's',
'采': 'c', '集': 'j', '输': 's', '出': 'c',
```

---

## 六、执行计划（分阶段）

### Phase 1：公共基础与工具支持（1 天）

- [ ] `utils.js`：新增 `PHYSICAL`、`equivNoiseBW`、`thermalNoise`、`rss`、`calcENOB`
- [ ] `tools-data.js`：注册 `smps-transformer`、`signal-chain` 模块
- [ ] `app.js`：更新拼音映射
- [ ] 全站 `node --check` 语法检查

### Phase 2：模块 1 — 万年历天气集成（1~2 天）

- [ ] `calendar.js`：增加 Geolocation API 调用与降级逻辑
- [ ] `calendar.js`：集成 Open-Meteo API（含 30 分钟缓存）
- [ ] `calendar.js`：WMO Weather Code 映射表（中文 + Emoji）
- [ ] `calendar.js`：天气面板 UI（桌面端 + 移动端适配）
- [ ] 测试：授权/拒绝/超时/无网络四种场景

### Phase 3：模块 2 — 隔离电源变压器计算器（2~3 天）

- [ ] `smps-transformer.js`：三种拓扑 Tab 框架与参数输入 UI
- [ ] `smps-transformer.js`：Flyback 核心计算逻辑
- [ ] `smps-transformer.js`：Forward 核心计算逻辑
- [ ] `smps-transformer.js`：Push-Pull 核心计算逻辑
- [ ] `smps-transformer.js`：磁芯数据库与 AP 法选型算法
- [ ] `smps-transformer.js`：公式展示框与警告提示
- [ ] 测试：边界值（D>0.5、Vin<Vout、极端功率）

### Phase 4：模块 3 — ADC/DAC 信号链精度计算器（3~4 天）

- [ ] `signal-chain.js`：链路方向切换（采集/输出）
- [ ] `signal-chain.js`：可增删级卡片 UI 框架
- [ ] `signal-chain.js`：五种级类型的参数面板
- [ ] `signal-chain.js`：总增益与总带宽计算
- [ ] `signal-chain.js`：各级噪声折合与 RSS 叠加
- [ ] `signal-chain.js`：INL/DNL/Vos/GainError 误差预算
- [ ] `signal-chain.js`：ENOB / NFCR / 绝对精度计算
- [ ] `signal-chain.js`：结果汇总面板与详细计算展开
- [ ] 测试：典型信号链（分压+运放+RC+ADC）手算验证

### Phase 5：联调与验证（1~2 天）

- [ ] 所有新增模块在 Chrome / Edge / 移动端 Safari 测试
- [ ] 断网测试：天气模块降级正常；其余模块 100% 离线可用
- [ ] 边界检查：输入 0、负数、超大值、空值不崩溃
- [ ] 回归检查：现有工具（日历、分压、DCDC 等）功能正常
- [ ] `node --check` 全站语法通过

---

## 七、验证标准

每阶段完成后执行：

1. **功能检查**：新增功能在目标浏览器正常可用，计算结果与手算/仿真一致
2. **离线检查**：断网后，除天气模块降级外，所有计算工具 100% 可用
3. **代码检查**：`node --check` 语法通过，无未使用变量/导入
4. **边界检查**：输入 0、负数、空值、超大值（如 1e12）时不崩溃、不显示 NaN/Infinity
5. **回归检查**：现有收藏/搜索/主题切换/农历显示等功能未受影响
6. **移动端检查**：375px 宽度下所有新增 UI 无重叠、可滚动、可操作

---

*v4.0 方案完成。建议按 Phase 1~5 逐步推进，每完成一个 Phase 后 review 一次。*

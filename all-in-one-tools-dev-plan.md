# All-in-One 工程师工具箱 — 整合开发方案

> 版本：v1.0  
> 日期：2026-05-12  
> 目标：将现有 8 个独立工具整合为统一单页应用，并扩展至 20+ 个模块

---

## 一、项目概述

### 1.1 背景
现有工具散落在 8 个独立文件中，UI 风格各异（从纯白卡片到深色毛玻璃），无统一入口，数据无法互通，维护成本高。

### 1.2 目标
- **整合**：将现有工具统一为单页应用（SPA），左侧导航一键切换
- **扩展**：基于电子工程师高频场景，新增 12+ 个计算/转换工具
- **体验**：统一视觉风格、响应式布局、支持 PWA 离线使用
- **可持续**：模块化架构，新增工具只需写一个 HTML 片段 + 注册一行配置

### 1.3 用户画像
- 硬件/电子工程师（主要）
- 嵌入式开发者
- PCB Layout 工程师
- 产线工艺/采购人员

---

## 二、现有工具梳理

| # | 工具名称 | 类型 | 核心功能 | 复用度 |
|---|---------|------|---------|--------|
| 1 | 万年历 | 通用 | 公历/农历、本地日程、JSON 导导入出 | 高 |
| 2 | 万能分压计算器 | 电路 | E24/E96 穷举匹配最优分压电阻 | 高 |
| 3 | 上升时间转换 | 信号 | Tr ↔ BW 双向换算（一阶 RC） | 高 |
| 4 | 走线电流+过孔电流 | PCB | IPC-2221 载流计算 | 高 |
| 5 | 阻抗计算器 | PCB | 微带线/带状线 Z₀ 计算 | 高 |
| 6 | PCB 工艺选择 | 工艺 | 表单生成工艺规范文本 | 中 |
| 7 | 本地传文件 | 网络 | WebRTC P2P 传文件+聊天 | 中 |
| 8 | 串口助手 | 嵌入式 | *.exe，需独立运行 | 低 |

> **串口助手处理策略**：因其为 Windows 可执行文件，无法在浏览器内运行，整合后作为"外部工具"卡片提供下载/打开指引，或未来考虑用 WebSerial API 重写为网页版。

---

## 三、新增工具规划（按优先级排序）

### 🔴 P0 — 电子工程师高频必备（首批新增）

| 工具 | 功能简述 | 技术要点 |
|------|---------|---------|
| **欧姆定律计算器** | V/I/R/P 四选二自动计算 | 实时联动，单位自动换算 mA/A/mΩ/kΩ |
| **LED 限流电阻** | 根据 Vf/If/Vin 计算限流电阻及功率 | 内置常见 LED Vf 预设值 |
| **LC/RC 谐振频率** | 计算谐振点、截止频率 | 支持低通/高通/带通 |
| **串并联电阻/电容** | R/Rt、C/Ct 互换计算 | 支持多颗器件输入 |
| **dB 换算器** | dBm/dBW/Vpp/Vrms/dBV 互转 | 支持自定义阻抗（50Ω/600Ω/75Ω） |
| **ADC/DAC 分辨率** | 根据位数和参考电压算 LSB、SNR、量化误差 | 支持 8/10/12/16/24bit |
| **运放增益计算** | 反相/同相/差分放大器参数计算 | 自动推荐 E24 电阻 |
| **电源功耗/效率** | Vin/Vout/Iout 算 Pin/Pout/损耗/效率/温升 | 线性/LDO/DCDC 模式 |
| **波特图/滤波器** | RC/RL/LC 滤波器截止频率、Q 值、阶数 | 可视化频率响应简图 |
| **定时器 PWM 计算** | 根据主频、预分频、ARR 算 PWM 频率和占空比 | 预设 STM32/ESP32/GD32 |

### 🟡 P1 — 实用增强（第二批新增）

| 工具 | 功能简述 |
|------|---------|
| **电阻色环识别** | 4/5/6 环电阻读值与反查 |
| **电感/电容单位换算** | pF/nF/μF/mF、nH/μH/mH/H 快速换算 |
| **热阻/散热计算** | θja/θjc、散热器选型、结温估算 |
| **电池续航估算** | 容量(mAh)、负载电流、休眠电流、续航时间 |
| **CRC 校验工具** | CRC8/16/32，常见多项式预设（Modbus/USB/XMODEM 等） |
| **进制/编码转换** | 2/8/10/16 进制、ASCII、Base64、URL Encode |
| **波特率误差计算** | 根据晶振频率和目标波特率计算实际误差百分比 |
| **晶振负载电容** | 根据晶体规格和寄生电容计算外部匹配电容 |
| **电平转换计算** | 分压/电平转换芯片选型、高低电平阈值判定 |
| **差分阻抗计算器** | 差分微带线/带状线、共模阻抗、耦合系数 |

### 🟢 P2 — 锦上添花（第三批）

| 工具 | 功能简述 |
|------|---------|
| **线规/电流对照** | AWG/SWG ↔ mm²，对应载流能力速查 |
| **BOM 成本估算** | 简单表格录入，自动汇总成本、用量 |
| **PCB 翘曲计算** | 基于 IPC 标准的翘曲率评估 |
| **螺丝/孔径对照** | M2/M2.5/M3… 螺丝与钻孔/焊盘尺寸速查 |

---

## 四、技术架构

### 4.1 选型理由

采用 **纯原生 HTML/CSS/JS**，无构建工具、无框架依赖。

- 工具本质是本地离线使用，无需服务器
- 用户可能直接双击打开文件，无法保证有 Node/npm 环境
- 单文件即可运行，便于复制到任意电脑或内网环境

### 4.2 整体架构

```
src/
├── index.html              # 入口：导航框架 + 模块加载容器
├── css/
│   ├── base.css            # 变量/重置/字体/动画
│   ├── layout.css          # 侧边栏/主内容区/响应式
│   └── components.css      # 卡片/输入框/按钮/表格/标签页
├── js/
│   ├── app.js              # 路由切换、模块注册、主题管理
│   ├── utils.js            # 公共函数：单位换算、E24/E96 生成、localStorage 封装
│   ├── components.js       # 公共 UI 组件：Tab、InputWithUnit、ResultBox
│   └── modules/
│       ├── calendar.js     # 万年历（原功能）
│       ├── divider.js      # 分压计算器
│       ├── risetime.js     # 上升时间转换
│       ├── current.js      # 走线/过孔电流
│       ├── impedance.js    # 单端阻抗
│       ├── diff-imp.js     # 差分阻抗（新增）
│       ├── pcb-spec.js     # PCB 工艺规范
│       ├── webrtc-chat.js  # 局域网传文件
│       ├── ohms-law.js     # 欧姆定律（新增）
│       ├── led-resistor.js # LED 限流（新增）
│       ├── lc-calc.js      # LC/RC 谐振（新增）
│       ├── r-c-series.js   # 串并联 R/C（新增）
│       ├── db-convert.js   # dB 换算（新增）
│       ├── adc-calc.js     # ADC 分辨率（新增）
│       ├── opamp-gain.js   # 运放增益（新增）
│       ├── power-eff.js    # 电源效率（新增）
│       ├── filter.js       # 滤波器/波特图（新增）
│       ├── pwm-timer.js    # PWM 计算（新增）
│       ├── resistor-color.js # 色环电阻（新增）
│       ├── unit-convert.js # 单位换算（新增）
│       ├── thermal.js      # 热阻散热（新增）
│       ├── battery.js      # 电池续航（新增）
│       ├── crc.js          # CRC 校验（新增）
│       ├── base-convert.js # 进制转换（新增）
│       ├── baud-error.js   # 波特率误差（新增）
│       └── crystal-load.js # 晶振负载（新增）
├── assets/
│   └── icons/              # SVG 图标（或内联）
└── manifest.json           # PWA 配置
```

### 4.3 核心设计模式

#### 模块注册机制
每个工具模块导出一个标准对象：

```javascript
// js/modules/ohms-law.js
export default {
  id: 'ohms-law',
  name: '欧姆定律',
  category: 'circuit',      // circuit / pcb / signal / general / network
  icon: '⚡',
  description: '电压、电流、电阻、功率四选二自动计算',
  render(container) {
    container.innerHTML = `...HTML...`;
    this.initLogic();
  },
  initLogic() { /* 绑定事件 */ }
};
```

#### 路由切换
- URL Hash 路由：`index.html#ohms-law`
- 首次加载仅渲染导航框架，点击导航后动态 `import()` 对应模块并执行 `render()`
- 支持浏览器前进/后退

#### 状态持久化
- 每个模块独立使用 `localStorage`，Key 前缀隔离：`tools_{moduleId}_*`
- 提供统一封装 `utils.storage.get/set`
- 支持全局"导出所有配置"和"导入所有配置"

---

## 五、UI/UX 设计规范

### 5.1 设计原则
- **工程师友好**：信息密度高、结果突出、输入即时响应
- **暗/亮双主题**：默认跟随系统，可手动切换（参考上升时间转换的暗色与万年历的亮色偏好）
- **响应式**：侧边栏在 <768px 时收为汉堡菜单，内容区单列

### 5.2 色彩系统（CSS 变量）

```css
:root {
  /* 亮色模式（默认） */
  --bg: #f6f7fb;
  --surface: #ffffff;
  --text: #1e293b;
  --text-secondary: #64748b;
  --primary: #2563eb;
  --primary-light: #eff6ff;
  --success: #10b981;
  --warning: #f59e0b;
  --danger: #ef4444;
  --border: #e2e8f0;
  --shadow: 0 4px 6px -1px rgba(0,0,0,0.1);
}

[data-theme="dark"] {
  --bg: #0f172a;
  --surface: #1e293b;
  --text: #f1f5f9;
  --text-secondary: #94a3b8;
  --primary: #60a5fa;
  --primary-light: #1e3a8a;
  --border: #334155;
  --shadow: 0 4px 6px -1px rgba(0,0,0,0.5);
}
```

### 5.3 布局结构

```
┌─────────────────────────────────────────────┐
│  ☰  All-in-One 工程师工具箱      🌙  ⚙️    │  Header
├──────────┬──────────────────────────────────┤
│ 📐 电路   │                                  │
│   ⚡ 欧姆 │   ┌──────────────────────────┐   │
│   💡 LED  │   │ 工具标题 + 简介           │   │
│   ...    │   ├──────────────────────────┤   │
│ ─────────│   │ 输入区  │  结果区           │   │
│ 📟 PCB   │   │         │                   │   │
│   ...    │   └──────────────────────────┘   │
│ ─────────│                                  │
│ 📡 信号  │   ┌──────────────────────────┐   │
│   ...    │   │ 详细说明 / 公式 / 示例    │   │
│ ─────────│   └──────────────────────────┘   │
│ 🌐 网络  │                                  │
│   ...    │                                  │
│ ─────────│                                  │
│ 📅 通用  │                                  │
│   ...    │                                  │
└──────────┴──────────────────────────────────┘
```

### 5.4 统一组件规范

| 组件 | 说明 |
|------|------|
| `ToolHeader` | 图标 + 标题 + 一句话描述 |
| `InputWithUnit` | 数字输入框 + 单位下拉（原生 select） |
| `ResultBox` | 大字号结果 + 复制按钮 + 单位 |
| `ParamGrid` | 2~4 列参数卡片布局 |
| `TabBar` | 工具内部模式切换（如微带线/带状线） |
| `FormulaBlock` | 公式展示块（KaTeX 可选，或纯文本） |
| `PresetButton` | 常用值快捷按钮 |
| `DetailPanel` | 可折叠的技术说明区 |

---

## 六、数据与存储策略

### 6.1 localStorage 命名规范
```
tools_state        → 全局状态（主题、最近使用、侧边栏折叠）
tools_calendar_v1  → 万年历事件数据
tools_pcb-spec_v1  → PCB 工艺表单草稿
tools_favorites    → 用户收藏的常用工具列表
```

### 6.2 全局导入/导出
设置页提供：
- **导出全部**：打包所有 `tools_*` Key 为 JSON 文件下载
- **导入全部**：上传 JSON 恢复（可选择覆盖或合并）

### 6.3 最近使用
自动记录最近访问的 5 个工具，显示在侧边栏顶部"常用"分组。

---

## 七、PWA 与离线支持

- 配置 `manifest.json`，定义名称、图标、主题色
- 使用 Service Worker 缓存核心资源（`index.html`、`css/*`、`js/*`）
- 各工具模块采用**懒加载** + **动态缓存**：首次访问后自动缓存，后续可离线使用
- 串口助手 `.exe` 作为外部链接，标记为"需下载"

---

## 八、开发计划（建议 3 个迭代）

### Phase 1：框架 + 迁移（预计 3~4 天）
- [ ] 搭建 `index.html` 骨架与 CSS 变量系统
- [ ] 实现侧边栏导航、路由切换、主题切换
- [ ] 迁移现有 7 个 HTML 工具到模块系统（串口助手除外）
- [ ] 统一现有工具的 UI 风格
- [ ] 编写 `utils.js`：E24/E96 生成、单位格式化、localStorage 封装

**里程碑**：现有功能 100% 可用，外观统一。

### Phase 2：核心新增工具（预计 4~5 天）
- [ ] 欧姆定律计算器
- [ ] LED 限流电阻
- [ ] LC/RC 谐振频率
- [ ] 串并联 R/C
- [ ] dB 换算器
- [ ] ADC/DAC 分辨率
- [ ] 运放增益
- [ ] 电源功耗/效率
- [ ] 滤波器/波特图
- [ ] PWM 定时器

**里程碑**：工具总数达到 17 个，覆盖 80% 日常需求。

### Phase 3：完善与扩展（预计 3~4 天）
- [ ] 电阻色环、单位换算、热阻、电池、CRC、进制转换、波特率误差、晶振负载
- [ ] 差分阻抗计算器
- [ ] 全局设置页（主题、数据导入导出、快捷键）
- [ ] PWA 配置与 Service Worker
- [ ] 搜索功能（侧边栏顶部搜索框，支持工具名和描述）
- [ ] 快捷键支持（如 `Ctrl+K` 搜索、`Ctrl+数字` 切换工具）

**里程碑**：工具总数 25+，具备搜索、PWA、数据管理能力。

---

## 九、关键技术实现细节

### 9.1 E24/E96 标准值生成（复用现有逻辑）
现有代码直接提取为 `utils.generateSeries(baseValues, max)`，供分压器、运放增益、滤波器等模块复用。

### 9.2 单位自动换算
```javascript
// utils.js
const units = {
  resistance: [['Ω',1], ['kΩ',1e3], ['MΩ',1e6]],
  capacitance: [['pF',1e-12], ['nF',1e-9], ['μF',1e-6], ['mF',1e-3]],
  frequency: [['Hz',1], ['kHz',1e3], ['MHz',1e6], ['GHz',1e9]],
  time: [['ps',1e-12], ['ns',1e-9], ['μs',1e-6], ['ms',1e-3], ['s',1]]
};

function autoUnit(value, unitType) { /* 返回 {value, unit} */ }
```

### 9.3 实时计算 vs 按钮计算
- **实时计算**：输入框 `oninput` 即时响应（欧姆定律、单位换算等简单计算）
- **按钮计算**：穷举/复杂计算避免阻塞 UI（分压器穷举、CRC 等），使用 `setTimeout`/`requestIdleCallback`

### 9.4 公式渲染
考虑到无依赖原则，公式使用 Unicode 字符 + CSS 样式呈现：
```html
<div class="formula">
  Z₀ ≈ 87 / √(εᵣ + 1.41) × ln(5.98H / 0.8W + T)
</div>
```
如需更精美，可内联极小体积的 KaTeX（~60KB），但不建议。

---

## 十、风险与注意事项

| 风险 | 缓解措施 |
|------|---------|
| WebRTC 传文件模块在整合后兼容性问题 | 保持独立逻辑，仅在主框架中包裹容器，不改动信令流程 |
| 万年历 localStorage Key 冲突 | 迁移时检查旧 Key，自动导入到新命名空间 |
| 移动端体验差 | 侧边栏可收起、输入框放大（`font-size: 16px` 防止 iOS 缩放）、触摸友好的按钮尺寸 |
| 单文件过大 | 模块懒加载（`import()`），首屏只加载框架 + 默认工具 |
| 浏览器兼容性 | 以 Chrome/Edge/Firefox 最新 2 个版本为目标，不使用过于前沿的 API |

---

## 十一、附录：工具分类与图标建议

| 分类 | 图标 | 包含工具 |
|------|------|---------|
| 电路计算 | ⚡ | 欧姆定律、LED 限流、分压器、串并联 R/C、运放增益、电源效率 |
| PCB 设计 | 📟 | 走线/过孔电流、单端阻抗、差分阻抗、PCB 工艺规范 |
| 信号/射频 | 📡 | 上升时间转换、LC/RC 谐振、滤波器、dB 换算、波特率误差 |
| 嵌入式 | 🔌 | ADC/DAC、PWM 定时器、CRC、进制转换、晶振负载、电平转换 |
| 网络传输 | 🌐 | 局域网传文件、串口助手（外链） |
| 通用工具 | 📅 | 万年历、电阻色环、单位换算、热阻散热、电池续航 |

---

## 十二、后续可扩展方向

1. **WebSerial 串口助手**：利用浏览器 WebSerial API 重写串口助手，彻底摆脱 `.exe`
2. **BOM 匹配**：对接常见元器件数据库，自动匹配型号、价格、库存
3. **GitHub Gist 同步**：将用户配置同步到 Gist，跨设备共享
4. **插件系统**：允许用户编写自定义工具（通过 Monaco Editor 编辑 JS 并运行）
5. **AI 助手集成**：接入 Kimi API，输入自然语言查询（如"帮我算个 3.3V 转 1.8V 的分压电阻"）

---

*方案结束。建议先确认技术架构和首批新增工具清单，即可进入 Phase 1 开发。*

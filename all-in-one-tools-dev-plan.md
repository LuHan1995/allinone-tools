# All-in-One 工程师工具箱 — 整合开发方案

> 版本：v2.0  
> 日期：2026-05-13  
> 目标：修复已知问题、增强用户体验、新增高价值工具

---

## 一、本轮工作范围

基于 v1.0 完成后的全面评估，本轮聚焦以下三方面：

1. **修复已知问题** — 消除代码重复、边界防护缺失、错误处理不完善
2. **增强核心体验** — 收藏功能、搜索增强、加载状态、PWA更新提示
3. **新增高价值工具** — RC延时计算器、电平转换计算器等工程师高频需求

---

## 二、问题修复清单

### 🔴 P0 — 必须修复

| # | 问题 | 文件 | 修复方案 |
|---|------|------|---------|
| 1 | **首页模块数据与 app.js 不同步** | `home.js` + `app.js` | home.js 不再维护独立 `HOME_MODULES`，改为从 `window.MODULES` 动态生成 |
| 2 | **分压计算器穷举阻塞主线程** | `divider.js` | 将 `runBruteForce` 移入 Web Worker，计算期间显示进度 |
| 3 | **WebRTC Token 解析无错误处理** | `webrtc-chat.js` | `handleToken()` 中 `JSON.parse(atob(raw))` 包裹 try-catch |
| 4 | **波特率计算 BRR=0 无防护** | `baud-error.js` | 增加 `brrInt <= 0` 校验，显示"波特率过高或晶振频率过低" |
| 5 | **utils.js import 重复** | `crystal-load.js` | 合并为单行 import |
| 6 | **搜索无结果无提示** | `app.js` | 搜索无匹配时显示"未找到相关工具"提示 |
| 7 | **ref/ 目录旧版文件冗余** | `ref/` | 删除旧版独立 HTML 文件（已整合到模块中） |

### 🟡 P1 — 建议修复

| # | 问题 | 文件 | 修复方案 |
|---|------|------|---------|
| 8 | **index.html 内联样式重复** | `index.html` | 移除 `.search-box input:focus` 内联样式，统一由 CSS 控制 |
| 9 | **home.js id 未转义** | `home.js` | 使用 `escapeHtml` 转义模块 id |
| 10 | **单位常量仍有重复定义** | `filter.js`, `unit-convert.js` 等 | 改为从 `utils.js` 导入 `R_UNITS`/`C_UNITS`/`L_UNITS` |

---

## 三、体验增强清单

### 🔴 P0 — 核心体验

| # | 功能 | 说明 |
|---|------|------|
| 11 | **工具收藏/星标** | 在导航项旁增加 ☆/★ 收藏按钮，收藏的置顶在"收藏"分组 |
| 12 | **模块切换骨架屏** | 路由切换时显示 loading 骨架，避免白屏 |
| 13 | **PWA 更新提示** | SW 检测到新版本时弹出 toast 提示用户刷新更新 |

### 🟡 P1 — 体验优化

| # | 功能 | 说明 |
|---|------|------|
| 14 | **搜索键盘导航** | 搜索结果支持 ↑↓ 箭头选择，Enter 确认 |
| 15 | **搜索无结果提示** | 搜索无匹配时显示友好提示 + 清除搜索按钮 |
| 16 | **设置页版本号动态化** | 从 app.js MODULES 数量或某处统一读取版本 |

---

## 四、新增工具规划

### 🔴 P0 — 高价值新增

| 工具 | 类别 | 功能简述 |
|------|------|---------|
| **RC 延时计算器** | 电路 | 根据 R/C 计算上升/下降到阈值电压的时间，τ=RC，支持自定义阈值百分比 |
| **电平转换计算器** | 电路 | 1.8V↔3.3V↔5V 分压电平转换，判断高低电平阈值是否兼容 |

### 🟡 P1 — 后续新增

| 工具 | 类别 | 功能简述 |
|------|------|---------|
| **MOS 管选型计算** | 电路 | 根据负载电流、开关频率计算驱动功率、Rg |
| **采样率/抗混叠滤波器** | 信号 | 奈奎斯特采样率、抗混叠 RC 参数推荐 |
| **铜箔电阻计算** | PCB | 根据走线长度/宽度/铜厚计算直流电阻 |

---

## 五、执行计划（分步骤）

### Step 1：问题修复（P0 修复项 1~7）
- 修改 `home.js` 消除数据重复
- 修改 `divider.js` 增加 Web Worker
- 修改 `webrtc-chat.js`、`baud-error.js`、`crystal-load.js`
- 修改 `app.js` 增加搜索无结果提示
- 删除 `ref/` 旧版文件

### Step 2：代码质量优化（P1 修复项 8~10）
- 清理 `index.html` 内联样式
- `home.js` 增加 escapeHtml
- 统一单位常量导入

### Step 3：体验增强（P0 增强项 11~13）
- 实现收藏功能（localStorage + UI）
- 实现模块切换骨架屏
- 实现 PWA 更新提示

### Step 4：新增工具（P0 新增项）
- 开发 RC 延时计算器模块
- 开发电平转换计算器模块
- 注册到 app.js

### Step 5：最终检查与测试
- 全模块功能冒烟测试
- 代码语法检查
- 静态检查（标签闭合、XSS防护）

---

## 六、验证标准

每步完成后由独立 Agent 执行以下检查：

1. **功能检查**：该步修改的所有功能正常可用
2. **代码检查**：`node --check` 语法通过，无未使用变量/导入
3. **回归检查**：已修复功能未引入新问题
4. **边界检查**：极端输入下不崩溃、不显示 NaN/Infinity

---

*计划更新完成。按 Step 1~5 逐步执行。*

---

# All-in-One 工程师工具箱 — v3.0 演进方案

> 版本：v3.0（草案）
> 日期：2026-05-20
> 目标：在 v2.0 基础上，补齐工程师高频刚需、提升数据可复用性、增强离线可靠性

---

## 一、本轮工作范围

基于 v2.0 已完成的收藏、骨架屏、PWA 提示、RC 延时、电平转换等功能，v3.0 聚焦以下四方面：

1. **可靠性加固** — 农历云端化后的离线兜底、API 并发控制、大计算量防卡死
2. **数据可复用** — 计算历史、参数预设、结果分享链接、跨设备数据同步
3. **工程师高频刚需** — 功耗预算、示波器参数、铜箔电阻、通信协议时序等
4. **体验细节打磨** — 单位记忆、公式可视化、帮助浮层、移动端适配

---

## 二、可靠性加固

### 🔴 P0 — 必须解决

| # | 问题 | 影响 | 修复方案 |
|---|------|------|---------|
| 1 | **农历完全依赖云端 API** | 离线或无网络时万年历不显示农历 | `calendar.js` 保留最小化本地 fallback：仅计算“月+日”简版中文（不显示闰月/节气），或显示缓存数据 |
| 2 | **日历切换月份并发 30+ 请求** | 快速翻页时浏览器并发受限、API 可能被限流 | 增加并发队列（每批 8~10 个），超出部分排队；同时优先渲染当月日期，前后填充格延后加载 |
| 3 | **divider.js 穷举大参数无进度** | 用户输入极端值时 Worker 计算过久，无感知 | Worker 增加 `postMessage({type:'progress', value:0.xx})`，UI 显示进度条 |
| 4 | **webrtc-chat.js 无信令中继** | 必须手动复制粘贴 Token，体验极差 | 增加可选的"剪贴板自动同步"模式（轮询读取剪贴板，检测到合法 Token 自动解析）；或提供二维码交换 |

### 🟡 P1 — 建议优化

| # | 问题 | 修复方案 |
|---|------|---------|
| 5 | **大计算模块无取消机制** | `divider.js` 翻页时发送 `worker.terminate()` 并重新创建 Worker |
| 6 | **sw.js 缓存策略过于激进** | 模块 JS 使用 `StaleWhileRevalidate` 正确，但可补充 `maxAge` 逻辑，避免长期不更新 |
| 7 | **极端输入导致 Infinity/NaN 散落** | 在 `utils.js` 增加 `safeNumber()` 包装器，统一处理除零、根号负数、log(0) 等场景 |

---

## 三、数据可复用（新功能）

### 🔴 P0 — 核心功能

| # | 功能 | 说明 | 存储方案 |
|---|------|------|---------|
| 8 | **各工具计算历史** | 每个模块底部增加"最近 10 次计算"折叠面板，显示输入参数+结果 | `localStorage`，key 为 `hist_${moduleId}` |
| 9 | **参数预设/模板** | 用户可保存当前输入为一组"预设"（如"5V→3.3V 分压"），下次一键加载 | `localStorage`，key 为 `preset_${moduleId}` |
| 10 | **结果分享链接** | 将当前工具的所有输入参数编码为 URL query（如 `#divider?r1=10k&r2=20k`），他人打开自动回填 | Hash + `URLSearchParams`，无需后端 |
| 11 | **全站数据云同步（增强导出）** | 设置页支持导出"完整备份"（所有 localStorage 数据 + 预设 + 历史 + 收藏），生成加密压缩文本；导入时选择性恢复 | 纯前端 AES 加密 + LZ-string 压缩 |

### 🟡 P1 — 体验提升

| # | 功能 | 说明 |
|---|------|------|
| 12 | **单位偏好记忆** | 用户在某工具选择过 "mA" 后，下次默认选中 "mA"，而非每次回到 "A" |
| 13 | **公式可视化** | 在结果区域下方增加 "展开公式" 按钮，显示本次计算使用的完整公式（含代入数值），便于写文档时复制 |
| 14 | **工具内帮助浮层** | 每个模块右上角增加 "?" 按钮，点击弹出该工具的使用说明、参数含义、常见误区 |
| 15 | **对比计算模式** | 支持"锁定当前结果"，修改参数后新旧结果并排显示，直观对比差异 |

---

## 四、新增工具规划（聚焦硬件/嵌入式高频刚需）

### 🔴 P0 — 高价值新增（建议优先）

| 工具 | 类别 | 功能简述 | 复杂度 |
|------|------|---------|--------|
| **功耗预算表** | 系统 | 可添加多行器件（名称/电压/电流/数量/占空比），自动汇总总功率、峰值电流、推荐电源功率裕量 | 中 |
| **示波器参数计算器** | 信号 | 输入采样率、存储深度、时基，互算三者关系；提示奈奎斯特极限 | 低 |
| **铜箔电阻计算器** | PCB | 输入走线长度、宽度、铜厚（oz）、温度，按 IPC-2152 估算直流电阻与温升 | 低 |
| **CAN 位时间计算器** | 嵌入式 | 输入 CAN 时钟、目标波特率，计算 BRP/TSEG1/TSEG2/SJW，给出采样点位置与误差 | 中 |
| **IEEE 754 浮点转换器** | 嵌入式 | 输入十进制小数，显示 32/64 位 IEEE 754 二进制/十六进制；支持反向解析 | 低 |

### 🟡 P1 — 有价值新增

| 工具 | 类别 | 功能简述 | 复杂度 |
|------|------|---------|--------|
| **采样率/抗混叠滤波器** | 信号 | 输入目标信号带宽，给出最小采样率、推荐抗混叠 RC 截止频率与阻容值 | 低 |
| **LDO 压差与功耗计算器** | 电源 | 输入 Vin/Vout/Iout/压差，计算耗散功率、结温、效率；推荐散热片 | 低 |
| **步进电机参数计算器** | 电机 | 输入转速(rpm)、细分、步距角，计算脉冲频率；反推转速 | 低 |
| **UART 数据帧时长** | 嵌入式 | 输入波特率、数据位、校验位、停止位，计算单帧字节时长、理论最大吞吐量 | 低 |
| **SPI 时钟与数据率** | 嵌入式 | 输入 SPI 时钟、模式、数据位宽，计算理论数据率；反推所需时钟 | 低 |
| **编码器分辨率换算** | 嵌入式 | PPR/CPR/线数/Lines 互转；配合减速比计算输出轴分辨率 | 低 |
| **保险丝/断路器选型** | 电源 | 输入工作电流、浪涌电流、环境温度，推荐熔断器规格与曲线类型 | 低 |
| **看门狗超时计算器** | 嵌入式 | 输入 WDG 预分频、重装载值、时钟，计算超时时间；反推寄存器配置 | 低 |
| **继电器驱动电路** | 电路 | 输入线圈电压/电阻，计算驱动电流；推荐三极管/MOS 型号与续流二极管 | 低 |
| **传输线反射系数** | 信号 | 输入源阻抗、负载阻抗，计算反射系数 Γ、VSWR、回波损耗 dB | 低 |
| **Modbus CRC 计算器** | 嵌入式 | 输入十六进制数据帧，计算 CRC-16 (Modbus)；支持校验已有帧 | 低 |
| **Bootloader 地址分配** | 嵌入式 | 输入 Flash 大小、APP 大小、页大小，自动计算 Bootloader/APP 起始地址、中断向量偏移 | 低 |

### 🟢 P2 — 锦上添花（长期 backlog）

| 工具 | 类别 | 功能简述 |
|------|------|---------|
| **Smith 圆图简化版** | 射频 | 输入阻抗 Z，在简化圆图上标出位置，给出串联/并联电容电感匹配建议 |
| **电容 ESR 影响分析** | 电源 | 输入电容容值/ESR/频率，计算阻抗幅值与相位，判断是否为低频主导 |
| **数字滤波器参数** | 信号 | 输入截止频率、采样率，给出 FIR/IIR 阶数估算与简单系数 |
| **H 桥驱动参数** | 电机 | 输入电压/电流/开关频率，计算死区时间、栅极驱动功率 |
| **天线长度计算** | 射频 | 输入频率，计算 1/4λ、1/2λ、5/8λ 天线长度；支持速度因子修正 |
| **相位噪声/抖动换算** | 信号 | 输入相位噪声 dBc/Hz @ offset，换算 RMS 抖动；支持不同带宽 |
| **堆栈/内存估算** | 嵌入式 | 输入任务数、每层调用栈深度、局部变量大小，估算最小 RAM 需求 |
| **BLDC 电机参数** | 电机 | 输入 KV 值、电压、负载，估算空载转速、反电动势 |

---

## 五、执行计划（建议分阶段）

### Phase 1：可靠性加固（2~3 天）
- `calendar.js`：增加离线 fallback + 并发队列
- `divider.js`：Worker 进度条 + 翻页取消
- `utils.js`：增加 `safeNumber()`
- `webrtc-chat.js`：剪贴板自动检测 Token

### Phase 2：数据可复用（3~4 天）
- 各模块接入 `hist_${moduleId}` 历史记录
- 实现 `preset_${moduleId}` 参数预设
- `app.js` 路由层实现 URL 参数解析与回填
- 设置页增强：加密导出/选择性导入

### Phase 3：P0 新增工具（5~7 天）
- 功耗预算表（交互表格，支持增删行）
- 示波器参数计算器
- 铜箔电阻计算器
- CAN 位时间计算器
- IEEE 754 浮点转换器

### Phase 4：P1 新增工具（按需分批）
- 每次迭代挑选 2~3 个工具开发
- 统一接入历史记录、预设、分享链接体系

### Phase 5：体验打磨（穿插进行）
- 单位偏好记忆
- 公式可视化
- 帮助浮层
- 对比计算模式

---

## 六、技术约束与注意事项

1. **保持纯前端**：所有新增工具仍使用原生 HTML/CSS/JS，无构建工具、无框架依赖
2. **ES Module 懒加载**：新增模块按现有模式注册到 `tools-data.js`，由 `app.js` 动态 `import()`
3. **离线优先**：除万年历农历 API 外，所有计算逻辑必须离线可用；API 失败时有明确降级
4. **移动端适配**：新增复杂表格类工具（如功耗预算）需确保在 375px 宽度下可横向滚动或折叠
5. **XSS 防护**：所有动态插入的 HTML 继续通过 `escapeHtml` 处理；URL 参数回填时需校验类型与范围
6. **localStorage 容量**：全站历史记录 + 预设可能占用较大空间，需定期清理（保留最近 50 条/模块）

---

## 七、验证标准

每阶段完成后执行：

1. **功能检查**：新增/修改功能在 Chrome / Edge / 移动端 Safari 正常可用
2. **离线检查**：断开网络后，除农历外所有工具正常计算
3. **代码检查**：`node --check` 语法通过，无未使用变量/导入
4. **边界检查**：输入 0、负数、超大值、空值时不崩溃、不显示 NaN/Infinity
5. **性能检查**： divider.js 大参数计算时页面不卡顿，进度条正常更新
6. **回归检查**：已修复功能未引入新问题，收藏/搜索/主题切换正常

---

*v3.0 草案完成。建议按 Phase 1~5 逐步推进，每完成一个 Phase 后 review 一次。*
